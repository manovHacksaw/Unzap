{
  "framework": "nextjs",
  "buildCommand": "prisma generate && node scripts/bundle-templates.mjs && NODE_OPTIONS='--max-old-space-size=6144' next build --webpack",
  "installCommand": "bun install",
  "functions": {
    "app/api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
