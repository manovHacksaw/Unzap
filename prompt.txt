Please help me conduct a comprehensive codebase audit and refactor across the entire application (including `apps/studio`, `apps/landing`, and any shared packages/components). My primary goal is to aggressively delete unnecessary/dead code, eliminate technical debt, consolidate duplicate logic, and fix minor issues, but strictly **WITHOUT breaking any existing functionality**.
Treat this as a production-level cleanup. Here are the specific guidelines for this application-wide refactor:
1. **Delete Dead & Unused Code:**
   - Scan the entire project for unused components, orphaned files, unused hooks, dead variables, and legacy commented-out code.
   - Remove any unused dependencies from `package.json` files.
   - Clean up unused or redundant UI components (e.g., duplicate shadcn components or leftover UI pieces from previous designs).
2. **Consolidate and DRY (Don't Repeat Yourself):**
   - The project currently has similar layouts and components across different routes (e.g., `apps/studio/app/lab` vs `app/studio/contract-lab`). Identify duplicated logic, duplicated utilities, or duplicated UI panels, and suggest merging them into shared, reusable components.
   - Consolidate multiple utility files if they serve the same purpose.
3. **Preserve Crucial Layout Architecture:**
   - We recently fixed the IDE layout to use a strict, fixed-viewport "live IDE" feel. **Do not touch or modify the flexbox constraints** (`flex-1`, `min-h-0`, `overflow-y-auto`, `overflow-hidden`, `h-screen`) on the main layout wrappers or sidebars. The left, middle, and right panels must continue to scroll independently.
4. **Optimize State Management & Renders:**
   - Review high-frequency state updates (like cursor tracking, compilation logs, or wallet states). Ensure they are properly memoized (`useMemo`, `useCallback`) to prevent full-page re-renders.
   - Extract large monolithic page files into logically separated, smaller components to improve readability and rendering performance.
5. **Maintain Core Functionality (No Breaking Changes):**
   - The application relies heavily on Cairo compilation (`useCairoCompiler`), browser-based file storage, Starknet wallet interactions, and history tracking. 
   - Ensure that no props, context providers, or data pipelines connecting these core systems are altered in a way that breaks them.
   - Do not change any smart contract compilation logic or API routes unless there is a clear, unhandled error.
Please review the repository step-by-step. Start by listing the major areas of technical debt or dead code you've found, and then provide the specific file changes and deletions needed to clean them up.